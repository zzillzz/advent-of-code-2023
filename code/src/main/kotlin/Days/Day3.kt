package Days

class Day3 {
    fun part1(input: List<String>) {
        var sum = 0
        for (i in input.indices) {
            for (j in i..<input[i].length) {
                var currentChar = input[i][j]
                if (currentChar.isDigit()) {

                }
            }
        }
    }
}

